首先对topic tree聚类数量的变化进行统计：
	1.预期找到（不设定相似度阈值的情况下）通过类数量变化得到停止条件。2,如果设定相似度阈值，找到设定该相似度的算法，否则不能判定聚类停止条件。


第二：
	对聚类后的结果进行tf-idf转换，对每一类的特征进行降维。由于topic，数量较少（维度远远大于数据个数），暂时不能确定pca效果如何。

第三：
	英文数据的获得，条件u随机场的了解。

